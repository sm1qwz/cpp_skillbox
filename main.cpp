

#include <iostream> 

int main() { 
int a = 42;
int b = 153;
std::cout << "a: " << a << "\n"; /* На экран будет выведено 42 */
std::cout << "b: " << b << "\n"; /* На экран будет выведено 153 */

std::swap(a, b); //способ использования через swap

// еще мой вариант через временное хранилище, выгодно ли так делать?
//int buff = a;
//a = b;
//b = buff;

std::cout << "a: " << a << "\n"; /* На экран будет выведено 153 */
std::cout << "b: " << b << "\n"; /* На экран будет выведено 42 */
}